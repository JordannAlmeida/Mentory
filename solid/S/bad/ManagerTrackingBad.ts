class ManagerTrackingBad {


    saveTrackinDateEvents(date: Date, status: number): void {
        //... save data into database
    }
    
    sendNotificationClient(clientId: String): void {
        //... send notification to client
    }
}