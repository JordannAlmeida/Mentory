class ManagerTrackingGood {

    saveTrackinDateEvents(date: Date, status: number): void {
        //... save data into database
    }

    getStatusTrackingByEventId(eventId: number):  number {
        //... find statusByEventId
        return 0;
    }
}